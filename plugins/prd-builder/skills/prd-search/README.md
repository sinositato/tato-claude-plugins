# PRD Search Skill

Full-text search across all PRD files in the project. Returns matching PRDs, the sections where matches occur, and surrounding context. Results are grouped by PRD and sorted by match count.

## Usage

```
/prd-search <query>
/prd-search authentication
/prd-search "error handling"
```

## Output

Terminal report (read-only, no files modified).
